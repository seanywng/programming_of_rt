) |>
plot_painted_canvas(palette = sample_canva(seed = 4))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .15)
) |>
plot_painted_canvas(palette = sample_canva(seed = 45))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .15)
) |>
plot_painted_canvas(palette = sample_canva(seed = 46))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .15)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(3 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .15)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .15)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2.3 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .15)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2.3 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .5)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2.3 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .433)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2.3 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .333)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2.3 + mf_noise) * (gate >= .52 & gate < .58) +
(5.1 + hf_noise) * (gate >= .333)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2.3 + mf_noise) * (gate >= .52 & gate < .58) +
(1.1 + hf_noise) * (gate >= .233)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .25)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .58) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .60) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .40) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .45) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .52 & gate < .5) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .32 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .35 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .45 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .25 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .15 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 25),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .1 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .05 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
# Corruption
blank_canvas |>
mutate(
lf_noise = gen_simplex(x, y, frequency = 1),
mf_noise = gen_simplex(x, y, frequency = 20),
hf_noise = gen_simplex(x, y, frequency = 99),
gate = gen_simplex(x, y, frequency = 10) |> normalise(),
paint = lf_noise +
(2 + mf_noise) * (gate >= .05 & gate < .55) +
(2.1 + hf_noise) * (gate >= .19)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
gen_sin <- function(x, frequency, ...) {
sin(x * frequency)
}
fracture(
x = 1:20,
noise = gen_sin,
fractal = fbm,
octaves = 8
)
dat <- tibble(
x = seq(0, 10, length.out = 1000),
y1 = fracture(x = x, noise = gen_sin, fractal = fbm, octaves = 1),
y2 = fracture(x = x, noise = gen_sin, fractal = fbm, octaves = 2),
y8 = fracture(x = x, noise = gen_sin, fractal = fbm, octaves = 8),
y20 = fracture(x = x, noise = gen_sin, fractal = fbm, octaves = 20)
)
ggplot(dat) + geom_path(aes(x, y1)) + ggtitle("One iteration")
ggplot(dat) + geom_path(aes(x, y2)) + ggtitle("Two iterations")
ggplot(dat) + geom_path(aes(x, y8)) + ggtitle("Eight iterations")
ggplot(dat) + geom_path(aes(x, y20)) + ggtitle("Twenty iterations")
custom_fracture <- function(x, ...) {
fracture(
gain = function(strength) {strength * .8},
frequency = function(frequency) {frequency * 1.3},
noise = gen_sin,
fractal = fbm,
x = x,
...
)
}
dat <- tibble(
x = seq(0, 10, length.out = 1000),
y1 = custom_fracture(x, octaves = 1),
y2 = custom_fracture(x, octaves = 2),
y8 = custom_fracture(x, octaves = 8),
y20 = custom_fracture(x, octaves = 20)
)
ggplot(dat) + geom_path(aes(x, y1,))
ggplot(dat) + geom_path(aes(x, y8))
ggplot(dat) + geom_path(aes(x, y20))
fractal_art <- function(fractal, generator, palette = NULL, ...) {
blank_canvas |>
mutate(
paint = fracture(
noise = generator,
fractal = fractal,
x = x,
y = y,
...
)
) |>
plot_painted_canvas(palette = palette)
}
fractal_art(fbm, gen_checkerboard, seed = 1, octaves = 1)
fractal_art(fbm, gen_checkerboard, seed = 1, octaves = 20)
fractal_art(fbm, gen_waves, seed = 1, octaves = 20)
blank_canvas |>
mutate(paint = gen_simplex(x, y, seed = 2)) |>
plot_painted_canvas()
View(plot_painted_canvas)
fractal_art(fbm, gen_simplex, seed = 2, octaves = 20)
fractal_art(ridged, gen_simplex, seed = 2, octaves = 20)
fractal_art(spectral_gain, gen_simplex, seed = 2, octaves = 20)
gf <- function(x) x * .8
fractal_art(ridged, gen_simplex, seed = 2, octaves = 20, gain = gf)
blank_canvas |>
mutate(paint = gen_worley(x, y, seed = 6)) |>
plot_painted_canvas()
fractal_art(billow, gen_worley, seed = 6, octaves = 8)
blank_canvas |>
mutate(paint = gen_worley(x, y, seed = 6, value = "distance")) |>
plot_painted_canvas()
View(plot_painted_canvas)
fractal_art(billow, gen_worley, seed = 6, octaves = 12)
blank_canvas |>
mutate(
paint = billow(
noise = gen_worley,
fractal = fractal,
x = x,
y = y,
...
)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
paint = billow(
noise = gen_worley,
fractal = fractal,
x = x,
y = y,
seed = 6,
octaves = 12
)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
blank_canvas |>
mutate(
paint = billow(
noise = gen_worley,
fractal = fractal,
x = x,
y = y,
seed = 6,
octaves = 12
)
) |>
plot_painted_canvas(palette = sample_canva(seed = 47))
fractal_art2 <- function(fractal, generator, palette = NULL, ...) {
blank_canvas |>
mutate(
paint = fracture(
noise = generator,
fractal = fractal,
x = x,
y = y,
...
)
)
}
fractal_art2(billow, gen_worley, seed = 6, octaves = 12)|>
plot_painted_canvas(palette = sample_canva(seed = 47))
fractal_art2(billow, gen_worley, seed = 6, octaves = 14)|>
plot_painted_canvas(palette = sample_canva(seed = 47))
fractal_art2(billow, gen_worley, seed = 6, octaves = 10)|>
plot_painted_canvas(palette = sample_canva(seed = 47))
gen_scope <- function(x, y, ...) {
worley_cell <- gen_worley(x, y, value = "cell", ...)
worley_dist <- gen_worley(x, y, value = "distance", ...)
return(normalise(worley_cell) + 5 * normalise(worley_dist))
}
pal <- sample_canva(seed = 2)
blank_canvas |>
mutate(paint = gen_scope(x, y, seed = 9)) |>
plot_painted_canvas()
blank_canvas |>
mutate(paint = gen_scope(x, y, seed = 9)) |>
plot_painted_canvas(palette = pal)
fractal_art(
billow, gen_scope, palette = pal, seed = 9, octaves = 8
)
pal_alt <- sample_canva(seed = 22)
fractal_art(
billow, gen_scope, palette = pal_alt, seed = 9, octaves = 13
)
fractal_art(
billow, gen_scope, palette = pal_alt, seed = 88, octaves = 13
)
fractal_art(
billow, gen_scope, palette = pal_alt, seed = 9, octaves = 13
)
gen_gate <- function(x, y, frequency, ...) {
lf <- gen_simplex(x, y, frequency = frequency, ...)
mf <- gen_simplex(x, y, frequency = frequency * 20)
hf <- gen_simplex(x, y, frequency = frequency * 99)
gate <- gen_simplex(x, y, frequency = frequency * 10, ...)
gate <- normalise(gate)
paint <- lf +
(mf + 2) * (gate >= .2 & gate < .8) +
(hf + 2) * (gate >= .1)
return(paint)
}
fractal_art(billow, gen_gate, palette = sample_canva(seed = 47), seed = 9, octaves = 1)
fractal_art(billow, gen_gate, palette = sample_canva(seed = 47), seed = 18, octaves = 1)
fractal_art(billow, gen_gate, palette = sample_canva(seed = 47), seed = 18, octaves = 18)
gen_gate2 <- function(x, y, frequency, ...) {
lf <- gen_simplex(x, y, frequency = frequency, ...)
mf <- gen_simplex(x, y, frequency = frequency * 20)
hf <- gen_simplex(x, y, frequency = frequency * 99)
gate <- gen_simplex(x, y, frequency = frequency * 10, ...)
gate <- normalise(gate)
paint <- lf +
(mf + 2.3) * (gate >= .52 & gate < .58) +
(hf + 1,1) * (gate >= .233)
paint <- lf +
(mf + 2.3) * (gate >= .52 & gate < .58) +
(hf + 1.1) * (gate >= .233)
gen_gate2 <- function(x, y, frequency, ...) {
lf <- gen_simplex(x, y, frequency = frequency, ...)
mf <- gen_simplex(x, y, frequency = frequency * 20)
hf <- gen_simplex(x, y, frequency = frequency * 99)
gate <- gen_simplex(x, y, frequency = frequency * 10, ...)
gate <- normalise(gate)
paint <- lf +
(mf + 2.3) * (gate >= .52 & gate < .58) +
(hf + 1.1) * (gate >= .233)
return(paint)
}
fractal_art(billow, gen_gate2, palette = sample_canva(seed = 47), seed = 18, octaves = 18)
fractal_art(billow, gen_gate2, palette = sample_canva(seed = 47), seed = 19, octaves = 18)
fractal_art(billow, gen_gate2, palette = sample_canva(seed = 47), seed = 19, octaves = 10)
paint <- lf +
(mf + 2.3) * (gate >= .52 & gate < .58) +
(hf + 6.1) * (gate >= .233)
gen_gate2 <- function(x, y, frequency, ...) {
lf <- gen_simplex(x, y, frequency = frequency, ...)
mf <- gen_simplex(x, y, frequency = frequency * 20)
hf <- gen_simplex(x, y, frequency = frequency * 99)
gate <- gen_simplex(x, y, frequency = frequency * 10, ...)
gate <- normalise(gate)
paint <- lf +
(mf + 2.3) * (gate >= .52 & gate < .58) +
(hf + 6.1) * (gate >= .233)
return(paint)
}
fractal_art(billow, gen_gate2, palette = sample_canva(seed = 47), seed = 19, octaves = 10)
paint <- lf +
(mf + 2.3) * (gate >= .52 & gate < .58) +
(hf + 3.1) * (gate >= .233)
gen_gate3 <- function(x, y, frequency, ...) {
lf <- gen_simplex(x, y, frequency = frequency, ...)
mf <- gen_simplex(x, y, frequency = frequency * 20)
hf <- gen_simplex(x, y, frequency = frequency * 99)
gate <- gen_simplex(x, y, frequency = frequency * 10, ...)
gate <- normalise(gate)
paint <- lf +
(mf + 2.3) * (gate >= .52 & gate < .58) +
(hf + 3.1) * (gate >= .233)
return(paint)
}
fractal_art(billow, gen_gate3, palette = sample_canva(seed = 47), seed = 19, octaves = 10)
gen_gate3 <- function(x, y, frequency, ...) {
lf <- gen_simplex(x, y, frequency = frequency, ...)
mf <- gen_simplex(x, y, frequency = frequency * 20)
hf <- gen_simplex(x, y, frequency = frequency * 99)
gate <- gen_simplex(x, y, frequency = frequency * 10, ...)
gate <- normalise(gate)
paint <- lf +
(mf + 2.3) * (gate >= .22 & gate < .58) +
(hf + 5.1) * (gate >= .233)
return(paint)
}
fractal_art(billow, gen_gate3, palette = sample_canva(seed = 47), seed = 19, octaves = 10)
